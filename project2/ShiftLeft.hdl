//CUSTOM gate
// File name: projects/02/ShiftLeft.hdl

/**
 * Shifts the all the bits one step to the left.
 * out = in when all the bits are shifted to the left.
 **/

CHIP ShiftLeft {
    IN in[16];
    OUT out[16];

    PARTS:
    Add16(a = in, b = in, out = out);	 
}